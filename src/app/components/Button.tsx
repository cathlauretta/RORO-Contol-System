import React from 'react'

export const Button = ({color, padding, text, onClick} : {color: string, padding: string | number, text : string, onClick: () => void}) => {
  return (
    <div>Button</div>
  )
}
